// @flow

let initialState = {
    user: {},
    errors: [],
};

function userReducer(state: any = initialState, action: any) {
    switch (action.type) {
        default:
            return state;
    }
}

export default userReducer;
